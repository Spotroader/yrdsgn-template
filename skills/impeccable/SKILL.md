---
name: impeccable
description: Redirect — this project uses the docs-only safe build. Load impeccable-safe instead.
---

# Moved to impeccable-safe

The full impeccable skill included Node scripts (live server, HTML injection, detect runners). Those triggered security scanner false positives and real risk if run unintentionally.

**Use instead:** [../impeccable-safe/SKILL.md](../impeccable-safe/SKILL.md)

Shared reference docs remain in `reference/` here for backward compatibility; prefer `impeccable-safe/reference/`.
